{
  "name": "neurobridge-backend",
  "version": "1.0.0",
  "description": "NeuroBridge API — server-side Gemini analysis, usage logging, and static frontend hosting (Cloud Run)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@google-cloud/firestore": "^7.11.0",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
